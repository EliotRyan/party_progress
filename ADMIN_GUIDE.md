# 管理员操作指南

本文档面向中国人民大学应用经济学院本科第一党支部的管理员，介绍如何使用入党积极分子电子档案系统。

---

## 一、登录

1. 访问系统网址，点击页面右上角的「管理」按钮
2. 输入您的**学号**和**密码**
3. 点击「登录」

> 初始密码由超级管理员分配。登录后建议立即修改密码。

登录成功后，页面顶部会显示您的姓名和角色，右上角按钮变为「退出管理」。

---

## 二、角色与权限

| 角色 | 可管理档案范围 | GitHub 配置 | 管理其他管理员 |
|------|---------------|------------|--------------|
| 超级管理员 | 全部学生 | ✅ 可配置并生成分享码 | ✅ |
| 党支部书记 | 全部学生 | ❌ 仅可导入分享码 | ✅ 可管理党务联系人 |
| 党务联系人 | 所在支部学生 | ❌ 仅可导入分享码 | ❌ |

---

## 三、档案管理

登录后默认进入「档案管理」标签页。

### 搜索与筛选

- **搜索**：在搜索框输入姓名或学号，实时过滤
- **按支部筛选**：通过「全部支部」下拉选择具体支部
- **按阶段筛选**：通过「全部阶段」下拉选择入党阶段

### 排序

点击表头列名可按该列升序/降序排列。

### 新增档案

1. 点击「➕ 新增」按钮
2. 在弹出的表单中填写信息：
   - **所在支部**为下拉选择（2020—2030 级 1班/数科班），不可自定义
   - 日期字段点击输入框可弹出日历选择器
3. 点击「添加」保存

### 编辑档案

1. 在表格中找到目标学生，点击行末「编辑」按钮
2. 修改信息后点击「保存」

### 删除档案

点击行末「删除」按钮，确认后删除。

### 批量操作

1. 勾选多行（或点击表头复选框全选）
2. 底部出现批量操作工具栏
3. 可**批量修改**：选择字段 → 输入新值 → 点击「应用」
4. 可**批量删除**：点击「批量删除」→ 确认

### 导出

点击「📥 导出」将当前档案下载为 `.xlsx` 文件。

### 保存到 GitHub

所有修改操作（新增/编辑/删除/批量修改）仅在当前浏览器生效。点击「💾 保存」按钮才会将数据推送到 GitHub 仓库。

---

## 四、数据导入

1. 切换到「数据导入」标签页
2. 选择导入模式：
   - **追加导入**：保留已有数据，添加新数据（默认）
   - **完全覆盖**：清空后导入（仅超级管理员可用）
3. 拖拽或点击上传 `.xlsx` 文件

> 要求 Excel 列名与系统字段一致：姓名、学号、所在支部、入党申请书日期、入党申请书页数、团支部推优日期、确定积极分子日期、确定发展对象日期、预备党员日期、正式党员日期、院党校培训、校党校培训

---

## 五、GitHub 配置

### 超级管理员配置

1. 切换到「GitHub 配置」标签页
2. 填写 **GitHub 用户名**、**仓库名**、**Token**（需有仓库写权限）
3. 点击「保存」
4. 点击「📤 生成分享码」，将生成的字符串复制给其他管理员

### 党支部书记 / 党务联系人配置

> 您不需要自行配置。只需从超级管理员处获取分享码。

1. 在档案管理工具栏点击「📥 导入配置」
2. 粘贴超管提供的分享码
3. 点击「📥 导入」

配置完成后即可使用「💾 保存」功能。

---

## 六、修改密码

1. 切换到「修改密码」标签页
2. 依次输入**当前密码**、**新密码**、**确认新密码**
3. 点击「修改」

密码将通过 GitHub API 写入 `admins.json`。

---

## 七、管理员管理（仅 super / secretary）

切换到「管理员管理」标签页：

- 党支部书记可添加、删除、重置党务联系人的密码。
- **添加管理员**：填写学号、姓名，选择角色。党支部书记仅可添加党务联系人；超级管理员可添加党支部书记或党务联系人。党务联系人需指定管辖支部。
- **重置密码**：将指定管理员密码重置
- **删除管理员**：移除管理员账号

---

## 八、常见问题

**Q：点击「💾 保存」失败？**
A：请确认已配置 GitHub（超管需自行填写 Token，其他管理员需导入超管分享的分享码）。

**Q：找不到某个学生？**
A：党务联系人只能看到自己管辖支部的学生。确认该学生的「所在支部」与您的管辖范围一致。

**Q：导入 Excel 后数据没变化？**
A：确认 Excel 列名与系统要求一致。检查是否选择了正确的导入模式。

**Q：忘记密码？**
A：联系超级管理员为您重置密码。